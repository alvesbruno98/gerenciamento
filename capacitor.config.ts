import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GerenciamentoDeInformacoes',
  webDir: 'www'
};

export default config;
